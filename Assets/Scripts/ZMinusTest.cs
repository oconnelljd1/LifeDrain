using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ZMinusTest : MonoBehaviour
{
    // Start is called before the first frame update
    void OnDisable()
    {
        Debug.Log("OnDisable");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
